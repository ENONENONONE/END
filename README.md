# END

START is the derivation. END is where it arrives. The two directories are the same claim read in opposite directions, which is itself the finding.

| File | Holds |
|---|---|
| `CONCLUSION.md` | The whole argument, START to the Voynich, in order |
| `THE_READING.md` | How the manuscript is to be read, as a procedure |
| `BERESHIT.md` | Genesis 1:1 worked through the reading: what it reaches, what it does not |
| `build.py` | Builds the pages; reads every figure from the data at build time |
| `solved/` | **The site for end.rito.one** — nine pages, `index.html` is the landing page |

## What was ingested

`~/Documents/START` (the local copy of start.rito.one) and its links:

- `README.md` — the derivation chain, Φ = ∂ₜΦ to the lattice
- `solver/synopsis.html` — the three papers as one argument
- `solver/math-derivation.html` — the axiom, the Fibonacci operator, det M = −1, the Pisot condition, the totient criterion
- `solver/unification.html`, `abstract.html`, `deductions.html` — Paper I and Paper II
- `solver/lattice-thesis.html` — Paper III: Giza, the golden angle, 109 sites, Rongorongo, the four-pole axis, the Younger Dryas hypothesis
- `solver/structural_comparison.html` — the four structural primitives and precession through the twelve rays
- `solver/cycles.html`, `timeline.html` — the 36 Myr node cycle
- `bholes.html` — the sky catalogue, 105 black holes, 94 stars, 88 radio sources

Against the corpus as it now stands: `emerald_tablet.json` at 1460 words, the keys, `mi25_readout.md` through LEDGER 71, and the Voynich work in `~/Documents/artifacts/voynich_2026-09-19/`.

## The finding in one line

The manuscript is read from the end to the beginning, each line right to left, because that is the direction of precession; the same reversal is what made the Rongorongo tablets speak in Paper III; and read that way the sixth entry from the last page carries **913 = BERESHIT** with **203**, the second word, following in the coordinate only the mirror produces.
